import cv2
import numpy as np

k = 5
for a in range(-k,k):
    print(a)
    
s = 3/2
print(int(s))
    
# temp = np.zeros([3, 3])  
# for l in range (0,3):
#     for c in range(0,3):
#         temp[l][c] = c
    
    
# print(np.mean(np.sort(temp, axis =None)))